server:
  port: 8086
spring:
  application:
    name: payment-service
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db?useSSL=false
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
